COMMIT=40d78aafa2ae4dda3024a0c6c6a0e360b2d69d9b
GOVERNED_FINGERPRINT=aee2427da83b43e5af76c3b0180c136cae265dfe1bdd813e4a2da3901872bf94
DIRECT_EXECUTION=python -m unittest discover -s tests -v
DIRECT_RESULT=10 tests PASS, exit 0
ORACLES:
- initialized Loomfile validates with zero errors/warnings
- every archive manifest entry byte count and SHA-256 matches its actual archived payload
- packaging leaves the project-side release-manifest record byte-identical
- fresh archive extraction preserves every required empty directory and validates with zero errors/warnings
- output inside the Loomfile is rejected before side effects and the project tree remains byte-identical
- existing archive refusal preserves archive and project bytes
- simulated competing output is not overwritten; project and competing bytes remain unchanged; temps are removed
- simulated mid-archive write failure leaves no final/temp archive, preserves the project, and permits retry
- injected unregistered-output mutation still produces manifest hashes for the exact bytes archived
- injected registered-source mutation after pre-validation is rejected by extracted-archive validation; no output/temp remains; retry succeeds after intentional source restoration
- os.link side-effect followed by KeyboardInterrupt removes only the owned output, preserves the project, removes temps, and permits retry
RAW_EXECUTION=verification/remediation-2026-08-12/raw/unit-tests.txt
VERDICT=PASS for ten directly executed package integrity, recovery, containment, archive-round-trip, concurrency, and retry regressions at this exact commit. No symlink-race, live-host, or publication claim.
